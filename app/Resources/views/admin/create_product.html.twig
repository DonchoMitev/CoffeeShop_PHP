{% extends 'base.html.twig' %}

{% block main %}
    <div class="container">
        <div class="col-md-4 col-md-offset-4">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-primary" formnovalidate>Add Product</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}

{% block body_class %}

{% endblock %}

